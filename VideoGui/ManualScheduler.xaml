<ui:FluentWindow x:Class="VideoGui.ManualScheduler"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VideoGui" ResizeMode="CanResizeWithGrip" 
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" Title="Manual Scheduler"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" 
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica" 
        mc:Ignorable="d" Loaded="Window_Loaded" Icon="/computer.ico" 

         WindowStartupLocation="CenterScreen" d:Background="gray"
         SizeChanged="Window_SizeChanged"
         SizeToContent="Manual" WindowStyle="ToolWindow" 
         Width="559" MinWidth="559" MinHeight="172" Height="182" 
         MaxWidth="559" MaxHeight="182">

    <ui:FluentWindow.Resources>
        <ResourceDictionary>
            <Style x:Key="DateTimePickerStyle1" TargetType="{x:Type xctk:DateTimePicker}">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="Wheat"/>
            <Setter Property="BorderBrush" Value="Wheat"/>
            <Setter Property="BorderThickness" Value="1,1,0,1"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="TimeWatermarkTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <ContentControl Content="{Binding}" 
                            Foreground="Wheat" Focusable="False" Margin="0,0,3,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="WatermarkTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <ContentControl Content="{Binding}" 
                          Foreground="Wheat" Focusable="False" Margin="0,0,3,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type xctk:DateTimePicker}">
                        <Border>
                            <Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <xctk:ButtonSpinner x:Name="PART_Spinner" 
                                      AllowSpin="{TemplateBinding AllowSpin}" 
                                      BorderBrush="Wheat" BorderThickness="{TemplateBinding BorderThickness}" 
                                      SpinnerLocation="{TemplateBinding ButtonSpinnerLocation}" 
                                      Background="White" 
                                      HorizontalContentAlignment="Stretch" 
                                      IsTabStop="False" 
                                      ShowSpinner="{TemplateBinding ShowButtonSpinner}" 
                                      VerticalContentAlignment="Stretch">
                                        <xctk:WatermarkTextBox x:Name="PART_TextBox" 
                                          AcceptsReturn="False" BorderThickness="0" 
                                          Background="Black" 
                                          Foreground="Wheat" 
                                          FontWeight="{TemplateBinding FontWeight}" 
                                          FontStyle="{TemplateBinding FontStyle}" 
                                          FontStretch="{TemplateBinding FontStretch}" 
                                          FontSize="{TemplateBinding FontSize}" FontFamily="{TemplateBinding FontFamily}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" IsUndoEnabled="True" MinWidth="20" Padding="{TemplateBinding Padding}" TextAlignment="{TemplateBinding TextAlignment}" TextWrapping="NoWrap" Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" TabIndex="{TemplateBinding TabIndex}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" WatermarkTemplate="{TemplateBinding WatermarkTemplate}" Watermark="{TemplateBinding Watermark}"/>
                                    </xctk:ButtonSpinner>
                                    <ToggleButton x:Name="_calendarToggleButton" 
                                      Grid.Column="1" Width="32" Height="28" Margin="2,0,0,0"
                                      Background="Black" BorderBrush="Wheat" Focusable="False"
                                      BorderThickness="1"
                                      IsChecked="{Binding IsOpen, RelativeSource={RelativeSource TemplatedParent}}" >
                                        <ToggleButton.IsHitTestVisible>
                                            <Binding Path="IsOpen" RelativeSource="{RelativeSource TemplatedParent}">
                                                <Binding.Converter>
                                                    <xctk:InverseBoolConverter/>
                                                </Binding.Converter>
                                            </Binding>
                                        </ToggleButton.IsHitTestVisible>
                                        <ToggleButton.IsEnabled>
                                            <Binding Path="IsReadOnly" RelativeSource="{RelativeSource TemplatedParent}">
                                                <Binding.Converter>
                                                    <xctk:InverseBoolConverter/>
                                                </Binding.Converter>
                                            </Binding>
                                        </ToggleButton.IsEnabled>
                                        <ToggleButton.Visibility>
                                            <TemplateBinding Property="ShowDropDownButton">
                                                <TemplateBinding.Converter>
                                                    <BooleanToVisibilityConverter/>
                                                </TemplateBinding.Converter>
                                            </TemplateBinding>
                                        </ToggleButton.Visibility>
                                            <Image Stretch="Fill" Source="/Icons/SelDown.png" />
                                        </ToggleButton>
                                </Grid>
                                <Popup x:Name="PART_Popup" IsOpen="{Binding IsChecked, ElementName=_calendarToggleButton}" StaysOpen="False">
                                    <Border BorderBrush="#FFABADB3" 
                                          BorderThickness="1" Padding="3">
                                        <Border.Background>
                                            <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                                                <GradientStop Color="CadetBlue" Offset="0"/>
                                                 <GradientStop Color="Aqua" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <StackPanel>
                                            <Calendar x:Name="PART_Calendar" BorderThickness="0" 
                                              DisplayDate="2016-05-24" 
                                              Style="{DynamicResource CalendarStyle1}"/>
                                            <xctk:TimePicker x:Name="PART_TimeUpDown" 
                                              AllowSpin="{TemplateBinding TimePickerAllowSpin}" 
                                              Background="Black" 
                                              ClipValueToMinMax="{Binding ClipValueToMinMax, RelativeSource={RelativeSource TemplatedParent}}" 
                                              Foreground="Wheat"
                                              FormatString="{TemplateBinding TimeFormatString}" 
                                              Format="{TemplateBinding TimeFormat}" 
IsUndoEnabled="{Binding IsUndoEnabled, RelativeSource={RelativeSource TemplatedParent}}" 
Kind="{Binding Kind, RelativeSource={RelativeSource TemplatedParent}}" 
Maximum="{Binding Maximum, RelativeSource={RelativeSource TemplatedParent}}" 
Minimum="{Binding Minimum, RelativeSource={RelativeSource TemplatedParent}}" 
ShowButtonSpinner="{TemplateBinding TimePickerShowButtonSpinner}" Text="" 
Visibility="{TemplateBinding TimePickerVisibility}" 
Value="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}" 
WatermarkTemplate="{TemplateBinding TimeWatermarkTemplate}" 
Watermark="{TemplateBinding TimeWatermark}"/>
                                        </StackPanel>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="Wheat"/>
                            </Trigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsReadOnly, RelativeSource={RelativeSource Self}}" Value="False"/>
                                    <Condition Binding="{Binding AllowTextInput, RelativeSource={RelativeSource Self}}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="IsReadOnly" TargetName="PART_TextBox" Value="True"/>
                            </MultiDataTrigger>
                            <DataTrigger Binding="{Binding IsReadOnly, RelativeSource={RelativeSource Self}}" Value="True">
                                <Setter Property="IsReadOnly" TargetName="PART_TextBox" Value="True"/>
                            </DataTrigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter Property="BorderBrush" Value="Wheat"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Wheat"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="FocusManager.FocusedElement" TargetName="PART_TextBox" Value="{Binding ElementName=PART_TextBox}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CalendarStyle1" TargetType="{x:Type Calendar}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black" Offset="0"/>
                            <GradientStop Color="Black" Offset="0.16"/>
                            <GradientStop Color="Black" Offset="0.16"/>
                            <GradientStop Color="Black" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FFA3AEB9" Offset="0"/>
                        <GradientStop Color="#FF8399A9" Offset="0.375"/>
                        <GradientStop Color="#FF718597" Offset="0.375"/>
                        <GradientStop Color="#FF617584" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Calendar}">
                        <Viewbox Width="200" Height="200">
                            <StackPanel x:Name="PART_Root" HorizontalAlignment="Center">
                                <CalendarItem x:Name="PART_CalendarItem" 
                                  BorderBrush="{TemplateBinding BorderBrush}" 
                                  BorderThickness="{TemplateBinding BorderThickness}" 
                                  Background="{TemplateBinding Background}" 
                                  Style="{TemplateBinding CalendarItemStyle}"/>
                            </StackPanel>
                        </Viewbox>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

       
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/FluentWindowControls.xaml"/>

            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ui:FluentWindow.Resources>
    <Grid Margin="0,0,0,1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Name="ManualSdcheduler" Grid.Row="0" Title="Manual Scheduler" Height="30">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="/Icons/computer.ico" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <Grid Grid.Row="1">
            <GroupBox x:Name="grpScheduleDates" Header="Video Scheduled Date &amp; Time" Canvas.Top="73" Margin="5,0,5,40" Width="549">
                <Canvas x:Name="cnvmain" Margin="0,0,0,0" Width="516" Height="52">
                    <CheckBox x:Name="chkSchedule" Content="Test Mode" Canvas.Top="34" Canvas.Left="419" 
                              HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,3,0"/>
                    <Label Name="lblDate" Content="Date :" HorizontalAlignment="Center"
                           Canvas.Top="18" VerticalAlignment="Top" Canvas.Left="6" MouseDoubleClick="lblDate_MouseDoubleClick"/>


                    <xctk:DateTimePicker x:Name="ReleaseDate" Format="Custom" 
                       FormatString="dd/MM/yyyy" MinWidth="140" Foreground='Bisque'
                       Focusable="True" Width="140" HorizontalAlignment="Center" 
                       Canvas.Left="45" Canvas.Top="6" VerticalAlignment="Top" 
                      
                       GotFocus="ReleaseDate_GotFocus" Initialized="ReleaseDate_Initialized"
                       AutoCloseCalendar="True" TimePickerAllowSpin="False" 
                       TimePickerShowButtonSpinner="False" TimePickerVisibility="Collapsed" 
                       Background="#FF383333">

                    </xctk:DateTimePicker>
                    <Label x:Name="lblTime" Content="Start" Canvas.Left="198" 
                           HorizontalAlignment="Center" VerticalAlignment="Top" Canvas.Top="4"/>
                    <xctk:TimePicker x:Name="ReleaseTimeStart" Format="Custom"  FormatString="hh:mm:ss tt" MinWidth="140"  
                      Focusable="True" Width="140" HorizontalAlignment="Center" Canvas.Left="232" 
                      Canvas.Top="-8" VerticalAlignment="Top" Background="#FF180B0B" 
                      Foreground="White" />
                    <xctk:TimePicker x:Name="ReleaseTimeEnd" Format="Custom" FormatString="hh:mm:ss tt" MinWidth="140"  
                      Focusable="True" Width="140" Canvas.Left="232" Canvas.Top="32" HorizontalAlignment="Left" 
                      VerticalAlignment="Center" Background="#FF0A0101" 
                      Foreground="Wheat"  />
                    <Label x:Name="lblTime_Copy" Content="End" Canvas.Top="33" Canvas.Left="198" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                    <TextBox x:Name="txtMaxSchedules" Width="85" Height="24" Canvas.Top="9" Canvas.Left="419"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Label x:Name="lblTime_Copy1" Content="Max" Canvas.Top="14" Canvas.Left="388" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                </Canvas>
            </GroupBox>


            <Button x:Name="BtnClose" Content="Close" Width="90" 
              Height="30" Margin="0,0,9,4" 
              HorizontalAlignment="Right" VerticalAlignment="Bottom" 
              Click="BtnClose_Click"/>
            <Button x:Name="btnAccept" Content="Accept" Width="90" 
              Height="30" Margin="10,0,0,4" VerticalAlignment="Bottom" 
              Click="btnAccept_Click"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
